PROJECT(dracoWrapper_swig)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(../ADE)
INCLUDE_DIRECTORIES(${JNI_INCLUDE_DIRS})


# set package and outdir
LIST(APPEND options -package com.draco.swig)
SET_SOURCE_FILES_PROPERTIES(dracoWrapper.i PROPERTIES SWIG_FLAGS "${options}")
SET(CMAKE_SWIG_OUTDIR ${CMAKE_BINARY_DIR}/swig)

SET_SOURCE_FILES_PROPERTIES(dracoWrapper.i PROPERTIES CPLUSPLUS ON)
SWIG_ADD_LIBRARY(${PROJECT_NAME} LANGUAGE java SOURCES dracoWrapper.i)